# API: `all_loment`

> 源: `loment/examples/all_loment.lomt` · 由 lomdoc 生成

## 枚举

### `enum Option`



- `Option::Some` (T)
- `Option::None`

### `enum Result`



- `Result::Ok` (T)
- `Result::Err` (E)

## 函数

### `fn write_str(fd: u64, s: str) -> i64`



### `fn write_dec(fd: u64, v: u32) -> ()`



### `fn max_of(a: T, b: T) -> T`



### `fn fib(n: u32) -> u32`



### `fn _start() -> ()`
