# API: `bytes`

> 源: `loment/examples/bytes.lomt` · 由 lomdoc 生成

## 枚举

### `enum Option`



- `Option::Some` (T)
- `Option::None`

### `enum Result`



- `Result::Ok` (T)
- `Result::Err` (E)

## 函数

### `fn load16(p: ptr, off: u32) -> u32`



### `fn load32(p: ptr, off: u32) -> u32`



### `fn store16(p: ptr, off: u32, v: u32) -> ()`



### `fn store32(p: ptr, off: u32, v: u32) -> ()`
