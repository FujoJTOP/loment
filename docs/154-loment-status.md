# 154 · Loment 100 里程碑状态矩阵

> 由 `tools/loment_status.py` 从 docs/145 生成 —— 请勿手改。
> 计数口径：`完成` = ✅；`部分` = ✅ 部分；`未达` = ⚠️；其余按未开始。

- 完成 **71/100** · 部分 **16** · 未达 **2** · 未开始 **11**

| 里程碑 | 名称 | 判据 | 状态 | 归类 |
|---|---|---|---|---|
| M1 | 字符串字面量与 UTF-8 字节视图 | `let s: str = "abc";` 转译/IR 双路径输出一致 | ✅ | 完成 |
| M2 | 字符串操作（`len`/`eq`/`concat`/切片视图） | 三个操作的双路径逐值一致 | ✅ 部分（`str_len`/`str_eq`/`str_byte`；`concat` 阻塞于 M15 堆分配） | 部分 |
| M3 | 只读切片 `&[T]` | 函数参数传切片，IR 用 `{ptr,len}` | ✅ | 完成 |
| M4 | 可变切片 `&mut [T]` | 原地写入经双路径一致 | ✅ | 完成 |
| M5 | 借用检查 v0（最小规则：不别名可变借用） | 3 个正例通过 + 3 个负例报错 | ✅ | 完成 |
| M6 | 泛型函数（单态化） | `fn max<T>(a: T, b: T) -> T` 双路径一致 | ✅ | 完成 |
| M7 | 泛型 struct/enum | `Pair<u32>` 双路径一致 | ✅ | 完成 |
| M8 | 接口/trait（静态派发） | 两个实现通过同一接口调用 | ✅ | 完成 |
| M9 | 错误处理 `Result` + `?` | 嵌套调用短路语义正确 | ✅ | 完成 |
| M10 | 内建 `Option`/`Result` 与 match 糖 | `if let` 形式解析并转译 | ✅ | 完成 |
| M11 | 模块/包系统（多目录、路径解析） | 三目录工程编译通过 | ✅ | 完成 |
| M12 | 可见性 `pub` 与 API 边界 | 私有符号跨模块访问报错 | ✅ | 完成 |
| M13 | 所有权最小规则（移动/借用/复制） | 移动后使用报错；Copy 类型放行 | ✅ | 完成 |
| M14 | no-alloc 子集审计（无堆分配） | 编译器报告分配点，no-alloc 模块为 0 | ✅ | 完成 |
| M15 | 可选堆分配器接口 | 显式 `alloc` 后运行通过 | ✅ | 完成 |
| M16 | 确定性析构（RAII） | Drop 顺序测试 | ✅ Rust 路径（`impl Drop`）；IR 待做 | 完成 |
| M17 | 无 use-after-free 静态检查（子集） | 5 个负例被拒 | ✅ | 完成 |
| M18 | 溢出/除零语义定规 + 检查模式 | 两种模式各一条用例，语义写进规范 | ✅ | 完成 |
| M19 | panic/abort 策略（no_std 友好） | 裸机目标下 panic 走 abort | ✅ | 完成 |
| M20 | 内联汇编与端口 I/O 原语 | 读写端口 demo 在 QEMU 中生效 | ✅ Rust 路径；IR 明确拒绝 | 完成 |
| M21 | volatile/原子操作原语 | 原子自增在多核下无丢失 | ✅ 单线程验证 | 完成 |
| M22 | 位域与打包结构 | 与 `.lom` 布局单源一致 | ✅（位域内建 `get_bits`/`set_bits`；打包布局由 L0 单源） | 完成 |
| M23 | 聚合类型 IR：struct（GEP） | `Blk` 在原生路径跑出同一结果 | ✅ | 完成 |
| M24 | 定长数组 IR | `sum_array`/`fill_incr` 双路径一致 | ✅ | 完成 |
| M25 | 枚举 IR（tagged union） | 无载荷/带载荷枚举双路径一致 | ✅ | 完成 |
| M26 | `match` 降级（switch + phi） | 穷尽性用例双路径一致 | ✅ | 完成 |
| M27 | 短路 `&&`/` |  | `（phi 修正） | 未开始 |
| M28 | 字符串/切片 IR | M1–M4 用例在原生路径通过 | 阻塞于 P1 | 未开始 |
| M29 | 泛型单态化 IR | M6/M7 用例在原生路径通过 | 阻塞于 P1 | 未开始 |
| M30 | 裸机目标 `x86_64-unknown-none` | 产出 `.o` 无 libc 依赖 | ✅ 部分（`-c` 出 1672 B 对象；链接流程未接） | 部分 |
| M31 | 无 libc 运行时（memcpy/memset 内联） | 链接后无未定义符号 | ✅ | 完成 |
| M32 | 自定义入口 + 链接脚本（与 FujoOS 对齐） | 产物能被 `kernel.ld` 布局吃下 | ✅ | 完成 |
| M33 | 中断/异常函数属性（naked/interrupt） | QEMU 中触发中断并返回 | ✅ 部分（`x86_intrcc` 就绪；IDT/QEMU 运行待 P7） | 部分 |
| M34 | 后端一致性差分回归 | IR 路径 vs Rust 路径全用例逐值一致 | ✅（native 7 函数逐值一致） | 完成 |
| M35 | `capability` 的运行时表示 | 生成物含域描述常量 | ✅（`CapDomain` 表 / `@__loment_caps`） | 完成 |
| M36 | 能力域静态检查 | 越界访问编译期拒绝 | ✅（`guard` 语句） | 完成 |
| M37 | 撤销语义代码生成（revocable） | 撤销后调用返回拒绝 | 部分（标志进域表/Potato；撤销由内核实施，P7） | 未开始 |
| M38 | 审计钩子（每次能力使用） | 审计条目数与调用次数一致 | ✅ | 完成 |
| M39 | 与 `kernel/src/capability.rs` 域模型对齐 | 双向 diff 0 差异 | 待做（内核侧改动，P7） | 未开始 |
| M40 | A1–A4 断言的 Loment 表达 | `inv_run` 自检通过 | 待做（P7） | 未开始 |
| M41 | `excluded` 出界声明强制 | 越界能力声明编译期报错 | ✅ | 完成 |
| M42 | 信任自适应域宽接口 | 域宽随质量台账变化可观测 | 待做（P7） | 未开始 |
| M43 | 能力域模糊测试 | 无越权放行 | ✅（120 组随机域/索引） | 完成 |
| M44 | 能力安全形式化说明 | docs 定稿并进论文素材 | ✅（docs/146） | 完成 |
| M45 | 形式对象 v1（泛型/切片/字符串） | 新类型全部导出且校验通过 | ✅ | 完成 |
| M46 | 编译器强制导出（不可关闭） | 缺形式对象则编译失败 | ✅ | 完成 |
| M47 | 独立校验器完备性 | 校验器不 import 编译器代码 | ✅ | 完成 |
| M48 | 结构识别转换率测量（波 C 主表） | 三语言 × 两模型对照表 | ✅ 部分（LLM 臂未运行） | 部分 |
| M49 | Python/C/Rust → Potato 转写工具 | 转写率与错误率可复现 | ✅ | 完成 |
| M50 | 形式对象 → 内核断言绑定 | A1–A4 由形式对象驱动 | ✅ 部分（内核接入归 P7） | 部分 |
| M51 | 形式对象版本化与回放 | 旧版本可回放校验 | ✅ | 完成 |
| M52 | Potato 规范文本（论文三素材） | 规范 + 测量协议成稿 | ✅ | 完成 |
| M53 | 跨实现一致性（FujoOS/LinuxFUAI） | 同一形式对象双实现同判定 | ✅ | 完成 |
| M54 | 测量协议与统计 | 置信区间与样本量定义 | ✅ | 完成 |
| M55 | 格式化器 `lomfmt` | 幂等：格式化两次结果相同 | ✅ | 完成 |
| M56 | LSP（补全/跳转/诊断） | 三个能力在编辑器实测 | ✅ 部分（无编辑器宿主，用 `handle()` 自测） | 部分 |
| M57 | 包管理器 `lompkg` | 依赖解析 + 校验和 | ✅ | 完成 |
| M58 | 文档生成器 `lomdoc` | 从 `.lomt` 生成 API 文档 | ✅ | 完成 |
| M59 | 调试信息（DWARF） | 调试器能按源码行断点 | ✅ 部分（语句级行表，无变量信息） | 部分 |
| M60 | IR 查看器 / 反汇编 | 一条命令看 IR 与机器码 | ✅ | 完成 |
| M61 | 内建测试框架 | `loment test` 跑通 | ✅ | 完成 |
| M62 | 基准框架 | 与 Rust 路径对比表 | ✅ | 完成 |
| M63 | IR 级覆盖率 | 覆盖率报告可复现 | ✅ | 完成 |
| M64 | 错误信息质量（含修复建议） | 10 类错误各有建议 | ✅（13 类） | 完成 |
| M65 | 增量编译 | 改动单文件重编译时间下降 | ✅ | 完成 |
| M66 | 构建缓存 | 冷/热构建时间对照 | ✅（82.6ms → 9.0ms） | 完成 |
| M67 | 第一个 Loment 用户态程序跑在 FujoOS | 输出与宿主一致 | ✅ | 完成 |
| M68 | 第一个 Loment 驱动（AHCI 子集） | 读扇区成功 | ✅ 部分（核心逻辑 + mock；真机寄存器块归内核侧） | 部分 |
| M69 | FUI 运行时用 Loment 重写一个控件 | 桌面渲染无回归 | ✅（Node 打包与 `lom/fuc.lom` 逐字节一致） | 完成 |
| M70 | 中断处理用 Loment | 键盘中断路径生效 | ✅ 部分（IR 形状；IDT 安装归内核侧） | 部分 |
| M71 | 内核模块 ABI（Loment ↔ Rust 互操作） | 双向调用通过 | ✅ 部分（入口/装载 ABI 形状） | 部分 |
| M72 | Loment 版 syscall 层 | 兼容矩阵不回归 | ✅（46/46 opcode 覆盖） | 完成 |
| M73 | 内存管理子系统（一个分配器） | 压力测试无泄漏 | ✅（固定块池 first-fit） | 完成 |
| M74 | 调度器钩子 | 上下文切换计数正确 | ✅ 部分（ABI 约定；未接调度器） | 部分 |
| M75 | Loment 版调试器（"不再读二进制"） | 按符号名断点 | ✅（符号化 + 源行映射） | 完成 |
| M76 | Loment 版 bootprobe | 取代 bootstrap 读二进制 | ✅ | 完成 |
| M77 | 内核自检用 Loment 写 | 自检项全绿 | ✅ | 完成 |
| M78 | 首个全 Loment 的 demo 程序 | 进回归矩阵 | ✅ | 完成 |
| M79 | Loment 版 lexer | 与 Python 版 token 流一致 | ✅ | 完成 |
| M80 | Loment 版 parser | AST 与 Python 版结构一致 | ✅ 部分（语句/表达式子集；见 docs/150） | 部分 |
| M81 | Loment 版类型检查 | 负例集判定一致 | ✅ 部分（4 条规则 + 单编译单元；见 docs/150） | 部分 |
| M82 | Loment 版 IR 生成 | `.ll` 与 Python 版逐字节一致 | ✅ 部分（常量/参数返回子集；见 docs/150） | 部分 |
| M83 | 自编译：编译器编译自身 | 产出可运行二进制 | 未开始 | 未开始 |
| M84 | 三阶段自举定点校验 | 第 2/3 阶段产物逐字节相同 | 未开始 | 未开始 |
| M85 | 自举编译器跑全部测试 | `lomentc_test` 在自举版上通过 | 未开始 | 未开始 |
| M86 | 自举性能优化 | 编译自身时间进入预算 | 未开始 | 未开始 |
| M87 | 引导脚本与发布包 | 干净环境一键引导 | ✅（`tools/loment_bootstrap.py`） | 完成 |
| M88 | 自举版本发布 | 打 tag + 校验和 | ✅ 部分（`SHA256SUMS` 101 行；tag 未推送） | 部分 |
| M89 | SDK：Loment 版示例集 | 10 个示例全通过 | ✅（25 个） | 完成 |
| M90 | 第三方库加载（L0 单源共享） | 外部库接入不复制常量 | ✅ | 完成 |
| M91 | 版本与兼容策略 | 语义化版本规则成文 | ✅ | 完成 |
| M92 | 跨平台目标（aarch64） | 交叉编译产物可运行 | ✅ 部分（交叉编译通过；无模拟器执行） | 部分 |
| M93 | 语言手册站点 | 手册与编译器同版本 | ✅ | 完成 |
| M94 | 教程与迁移指南 | Rust → Loment 迁移案例 | ✅ | 完成 |
| M95 | 发布流程与社区规范 | 发布检查单 | ✅ | 完成 |
| M96 | 语言稳定性承诺（1.0 冻结） | 冻结后 API 不再破坏 | ⚠️ 未冻结（1.0-pre） | 未达 |
| M97 | 语言设计与实现的论文素材 | 设计决策有据可查 | ✅ | 完成 |
| M98 | 与 Rust/C/Zig 的形式化对比 | 对比矩阵成文 | ✅ | 完成 |
| M99 | 端到端可复现实验包 | 第三方机器可复现 | ✅（98 工件 sha256） | 完成 |
| M100 | 1.0 发布与审计 | 全门禁绿 + 外部审计 | ⚠️ 未达（1.0-pre） | 未达 |

## 剩余工作（按依赖）

- **M2** 字符串操作（`len`/`eq`/`concat`/切片视图） —— ✅ 部分（`str_len`/`str_eq`/`str_byte`；`concat` 阻塞于 M15 堆分配）
- **M27** 短路 `&&`/` —— `（phi 修正）
- **M28** 字符串/切片 IR —— 阻塞于 P1
- **M29** 泛型单态化 IR —— 阻塞于 P1
- **M30** 裸机目标 `x86_64-unknown-none` —— ✅ 部分（`-c` 出 1672 B 对象；链接流程未接）
- **M33** 中断/异常函数属性（naked/interrupt） —— ✅ 部分（`x86_intrcc` 就绪；IDT/QEMU 运行待 P7）
- **M37** 撤销语义代码生成（revocable） —— 部分（标志进域表/Potato；撤销由内核实施，P7）
- **M39** 与 `kernel/src/capability.rs` 域模型对齐 —— 待做（内核侧改动，P7）
- **M40** A1–A4 断言的 Loment 表达 —— 待做（P7）
- **M42** 信任自适应域宽接口 —— 待做（P7）
- **M48** 结构识别转换率测量（波 C 主表） —— ✅ 部分（LLM 臂未运行）
- **M50** 形式对象 → 内核断言绑定 —— ✅ 部分（内核接入归 P7）
- **M56** LSP（补全/跳转/诊断） —— ✅ 部分（无编辑器宿主，用 `handle()` 自测）
- **M59** 调试信息（DWARF） —— ✅ 部分（语句级行表，无变量信息）
- **M68** 第一个 Loment 驱动（AHCI 子集） —— ✅ 部分（核心逻辑 + mock；真机寄存器块归内核侧）
- **M70** 中断处理用 Loment —— ✅ 部分（IR 形状；IDT 安装归内核侧）
- **M71** 内核模块 ABI（Loment ↔ Rust 互操作） —— ✅ 部分（入口/装载 ABI 形状）
- **M74** 调度器钩子 —— ✅ 部分（ABI 约定；未接调度器）
- **M80** Loment 版 parser —— ✅ 部分（语句/表达式子集；见 docs/150）
- **M81** Loment 版类型检查 —— ✅ 部分（4 条规则 + 单编译单元；见 docs/150）
- **M82** Loment 版 IR 生成 —— ✅ 部分（常量/参数返回子集；见 docs/150）
- **M83** 自编译：编译器编译自身 —— 未开始
- **M84** 三阶段自举定点校验 —— 未开始
- **M85** 自举编译器跑全部测试 —— 未开始
- **M86** 自举性能优化 —— 未开始
- **M88** 自举版本发布 —— ✅ 部分（`SHA256SUMS` 101 行；tag 未推送）
- **M92** 跨平台目标（aarch64） —— ✅ 部分（交叉编译通过；无模拟器执行）
- **M96** 语言稳定性承诺（1.0 冻结） —— ⚠️ 未冻结（1.0-pre）
- **M100** 1.0 发布与审计 —— ⚠️ 未达（1.0-pre）
