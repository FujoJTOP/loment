# 177 · 兼容库收口（到 10 个语言）+ 新内容起步

> 2026-09-17 用户指示：**"把我说的全部做完，写 plan 自己做，做完之前不要回复我"** ——
> ① 将兼容库**彻底结束**，可以调用 **10 个语言左右**的库；② 开始新内容。
> 上游：`docs/173`（FFI 四阶段）· `docs/175`（0.1.4 方向）· `docs/176`（改冻结面的代价）
> 本文件是**执行计划 + 结果记账**。

## 0. 侦察结论（决定计划形状）

| 事实 | 影响 |
|---|---|
| WSL = Ubuntu 26.04，`apt` 可达但 **sudo 需要密码** | **装不了包** → 工具链只能走"下载免安装 tarball 到 /tmp" |
| WSL 有 `curl` `wget` `tar` `xz` `make` `cc`；HTTPS 到 ziglang.org / adoptium / github 均 **200** | Zig、JDK、Lua 可拿 |
| WSL 已有 `python3` `node` `perl` `gcc` `g++` | Perl 是**白捡**的第 6 个语言 |
| 主机有 `clang` `clang++` `rustc` `go` `dotnet` `java` | 腿 1 的交叉编译从主机侧走 |
| WSL 无 `java` `ruby` `lua` `zig` `gfortran` | 前三个可下载/自建；gfortran 无预编译包，列后备 |

## 1. 为什么"彻底结束"的核心是**归档 + 重定位**，不是加更多语言

今天的 FFI 只吃**一个自包含的 `.o`**：不许有重定位、不许有未定义符号（`docs/173` §3）。

**真实的 C 库不是这个形状。** 真实库是**多目标文件的归档**（`ar rcs libfoo.a a.o b.o`），
成员之间**互相引用**，每个成员**都有重定位**。所以：

- 现在能调的只是"手工挑出来的单函数玩具"；
- "彻底结束兼容库" = 让**标准形状的静态库**能链 —— 这正是 `docs/173` 的**阶段 2**。

**所以计划的第一块是机制（它同时是 C 真正可用的前提），第二块才是语言广度。**

## 2. 目标集（10 个语言）

| # | 语言 | 腿 | 需要什么 | 状态 |
|---|---|---|---|---|
| 1 | **C** | 1 | 已有 | ✅ |
| 2 | **C++** | 1 | 已有 | ✅ |
| 3 | **Rust** | 1 | 已有 | ✅ |
| 4 | **Zig** | 1 | 下载 tarball → 单个 `.o` | ⬜ |
| 5 | **Go** | 1 | 归档 + 重定位（`-buildmode=c-archive`） | ⬜ |
| 6 | **Python** | 2 | 已有 | ✅ |
| 7 | **JavaScript** | 2 | 已有 | ✅ |
| 8 | **Perl** | 2 | 解释器已在 WSL → 只差判据 | ⬜ |
| 9 | **Lua** | 2 | `make linux` 自建（cc 已在） | ⬜ |
| 10 | **Java** | 2 | 下载 JDK tarball | ⬜ |

**达到 10 的两条路互不依赖**：机制（4/5）与广度（8/9/10）可以并行推进；
任一路走不通都不至于让整件事归零。

## 3. 执行块

### P0 —— `docs/176` 的 A（代价≈0，先做）

- [ ] `.gitattributes`：`loment/build/selfhost_driver.ll` 标成生成物（GitHub 折叠）
- [ ] `CLAUDE.md` 写进"语言面改动一律两条提交（手写 / 机械）"的纪律
- [ ] 判据：机械提交只改 `build/` 下的文件

### P1 —— 链接器机制补完（"彻底"的正题）

两个实现**逐字节一致**仍是硬要求（`docs/158` §5）。范围：

- [ ] **重定位**：`R_X86_64_PC32`(2) / `PLT32`(4) / `64`(1) / `32S`(11) / `32`(10) / `PC64`(24)
- [ ] **归档**：`ar` 格式（`!<arch>\n` + 成员头 60 字节）、`/` 符号索引、成员选择
- [ ] **跨对象符号解析**：所有对象/成员的**定义**先汇总，再解引用；
      仍然"解不开就硬拒"（不猜地址）
- [ ] 判据：**多目标文件的 C 静态库**（`a.o` 调 `b.o`）端到端跑通

**顺序**：先重定位（单对象内的 `PC32`），再归档（多对象），最后跨对象引用。
每一步都能独立证伪：把重定位处理去掉 → 判据必须红。

### P2 —— 语言广度（每加一个语言 = 一条配方 + 一条判据）

- [ ] **Perl**：`proc_sh("perl -e ...")`（解释器已在）
- [ ] **Lua**：自建 → `proc_sh("lua -e ...")`
- [ ] **Java**：下载 JDK → `proc_sh("javac H.java && java H")`
- [ ] **Zig**：下载 tarball → `zig build-obj` → `--link`
- [ ] **Go**：归档支持后试 `-buildmode=c-archive`；**若它的运行期/构造器不可解，如实记为"不可用"并写明缺什么**

### P3 —— 文档收口

- [ ] `docs/173` §5b 状态表、§2 阶段表（阶段 2 归档已做则改）、语言表
- [ ] `docs/174` 或新版发版说明：本轮的边界与不主张
- [ ] `docs/158` §4 若新增参考/自举差异，逐条记

### P4 —— 开始新内容（`docs/175`）

**前置**：`docs/176` A 做完（P0）。B0 顺带做掉 `docs/175` §4 的契约问题。

- [ ] `docs/176` **B0**：`--dump-surface` → `surface_data.lomt`，
      **先只放 L0 原语清单 + freestanding/hosted 标记**
- [ ] `choose` 进语言：至多一次、默认 `std`、库不许 `choose`、
      进 Potato 形式对象（`docs/175` §3）
- [ ] 判据：写两次报错 / `no_std` 引 hosted 原语报错并带来源链

## 4. 不主张（这一轮明确不做）

- 不做 `docs/176` 的 **C**（分层冻结是口径变更，要 compat 线同意）。
- 不做 `docs/176` 的 **B1**（内建表上生成管线）—— 除非 P1–P3 都收口还有余量。
- 不做多语法**前端**（`docs/175` §5）。它是 0.1.4 的后半程，前置是 `choose` + std。
- 不做动态库（`.so`/`.dll`）与运行期嵌入（CPython/JNI）—— 阶段 3/4。
- 不动 `lom/*.lom`。

## 5. 记账规则

每一块落地时：判据绿 → 提交（手写与机械**分开**）→ 更新本文件的勾。
**本文件在全部做完之前不进发布清单。**
