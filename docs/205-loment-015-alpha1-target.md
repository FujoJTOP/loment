# 205 · 0.1.5 Alpha1 目标：把 `docs/204` 的每一条优点做出**裁决**

> 状态: **目标账**（2026-09-23）· 输入: `docs/204`（C / Rust / Java / Python 的优点收集）
> 一句话: 用户定的目标是「0.1.5 Alpha1 要完成这所有的优点集合」——
> **按字面不可达**（下面 §0 举三条硬冲突），所以把它写成可证伪的形式：
> **每一条优点都要有一个裁决 + 一条判据，一条都不许停在"没表态"**。

## 0. 这个目标怎么读

### 0.1 为什么"全部实现"按字面不成立

`docs/204` §5 那张对照表列的就是**互相排斥**的路。三条硬冲突，各举一例：

| 冲突 | 一边 | 另一边 |
|---|---|---|
| **抽象机的厚薄** | C：薄到可以 UB —— 因为语言**不假设**任何东西，才换得到性能与可移植 | Rust：厚到能在编译期判借用 —— 因为语言**假设**了很多，才换得到静态证明。两者不可能同时最大化 |
| **智能放在哪儿** | Java：把智能放进运行期（GC + JIT），换取峰值与运维简化 | Loment 的立身之本是**无运行时、确定性**（`docs/140` 的差异点；`docs/175` §3.4 已经把 GC 划成两档）。把收集器装进核里＝放弃差异点 |
| **名字什么时候被解析** | Python：运行时解析，零仪式，写得快 | Java：编译期按名检查，工具与重构可靠。同一个 `use` 不可能既在编译期查又不在编译期查 |

### 0.2 于是写成什么

**0.1.5 Alpha1 的"完成" = `docs/204` 的每一行都有裁决、都有判据、且判据能跑。** 三种裁决：

| 裁决 | 含义 |
|---|---|
| **抄** | 按 204 里写的那条**机制**做（不是抄它的形容词），判据写清 |
| **换形式抄** | **要那个目的，不要那个形式**（例：受检异常的*意图*要，*形式*不要） |
| **明确不抄** | 写清撞的是哪条冲突、我们选了哪一边 —— **不抄也要有理由，理由就是它的判据** |

> **这条规矩本身就是它的判据**：表里出现"未定"、"待议"、"视情况"三个词的任何一行，
> 都算 0.1.5 Alpha1 没完成。**零沉默。**

---

## 1. 总表

量级那一列：**已有** = 今天已经成立、只差把判据钉紧；**一格** = 一轮内能做完；
**一轮** = 一次独立改动（走 `docs/158` §5 那四条）；**多轮** = 要开新的面。

### 1.1 C（6 条）

| # | 优点（`docs/204` §1） | 裁决 | 0.1.5 Alpha1 的判据（可证伪） | 量级 |
|---|---|---|---|---|
| C1 | 薄抽象机 | **抄**（已是立身之本） | 产物无未定义符号、不链 libc：`llvm-nm` 无 `U`，且种子→stage1→产物**全程无 clang**（C19/C20 已有） | 已有 |
| C2 | **C ABI 是通用货币** | **抄，但补反向那一半** | 今天只有"Loment 调 C"（`extern fn` + `--link`）。**新增：C 能调 Loment** —— 一个 C 程序 `extern` 一个 Loment 编出的函数并跑通（v0 的调用约定是自家的，原生产物**不给 C 调**） | 一轮 |
| C3 | 编译器可达性 | **抄** | 交叉目标从"只到发射"推进到**能执行**：aarch64 目标在 qemu 或真机上跑出一条断言（今天 `loment_p9_test` 的执行那格是 SKIP） | 一轮 |
| C4 | 小到能完整掌握 | **抄** | **规范与实现逐条对得上**：每一条诊断码、每一个内建，在 `docs/manual` 里都有一句可判定的写法 —— 判据是**机械核对**（抽码/抽内建，逐条找规范句） | 一格 |
| C5 | 显式内存与指针 | **换形式抄** | 指针要、**无边界不要**：越界访问在字面量时编译期拒、非字面量时运行期 trap（已有）；增量 = `alloc`/`free` 的确定性（见 J3） | 已有 + J3 |
| C6 | "不作主张"的谦虚 | **抄** | 分层：核不作主张，主张在契约层 —— `lom/*.lom` 的契约能被外部逐条核对（`fuai_contract` 已有） | 已有 |

### 1.2 Rust（7 条）

| # | 优点（`docs/204` §2） | 裁决 | 0.1.5 Alpha1 的判据（可证伪） | 量级 |
|---|---|---|---|---|
| R1 | 所有权 + 借用检查 | **换形式抄**（**明确不做** NLL 级别的引用图） | 移动后使用 / 返回悬垂 / 可变别名在子集内报错（M5/M13/M17 已有）。**边界已成文（2026-09-23）**：`docs/206-loment-ownership-boundary.md` —— 证的**三条**（E007 可变别名 / E006 移动后使用 / E012 悬垂借用）与**不证的四条**（不看生命周期、不跨函数、无字段级移动、移动集不分分支），每条都指到 `lomentc.py` 里的哪个函数。**那四条"不证"才是这一页的价值** —— 它写的是"Loment 不是 Rust 那套借用检查"。收进 `loment_manual.SPECS`，出现在手册的「规范」一栏 | 已有（+ 文档，已完成） |
| R2 | `Result` + `?` | **抄** | 早退语义正确（M9/M10 已有） | 已有 |
| R3 | 穷尽 match | **抄** | 漏臂是编译错误（M10 已有） | 已有 |
| R4 | trait + 单态化 | **抄，并补完自举侧** | 自举侧 M6/M7 的收窄面清空：判据是 `loment_potato_emit_test` 逐字节孪生全绿。**进度（2026-09-23，三个轴都落地）**：字段访问实参（`native_gen.lomt`）、**链式泛型**（M6 跑到**不动点**，`native_chain.lomt`）、**嵌套泛型实参**（`docs/147` §2 定了命名规则"**分隔长度 = 嵌套深度**"，`Outer<Inner<u32>>` → `Outer_Inner__u32`，两个实现一起改）—— **45 份逐字节相同**，`REFUSED` 已空。⚠ 第三条的来历：**补夹具时先把原判推翻了**（参考实现自己也发不出来），所以它当时**不是**"自举侧差一轮"而是"格式没定义" | 多轮（剩：**方言**在驱动器层；实参形状那半边 —— 数组/切片/指针当实参 —— 仍点名拒，但 2026-09-23 查清楚**卡的不是命名而是后端 `native M23`**（struct 字段暂只支持标量），`Box<ptr>` 是证据：名字没问题、IR 照样拒） |
| R5 | **`unsafe` 是词法边界** | **抄意图，形态要定** | 概念：越过语言保证的每一步都**在词法上显式、且可计数**。判据：`loment stat`（或新命令）能报出每个单元的**边界操作数**（`extern fn` 调用点 / `syscall4` / 裸 `ptr` 变换），且该计数在 Potato 对象里。<br>**已落地（2026-09-23）。**<br>**① `loment stat`** 多一节 `Boundary operations`（五个数）。`unsafe` 在 Loment 里**不是关键字**，所以这条边界由**名字**划：`potato.BOUNDARY_BUILTINS` 是那份单一真源（`lomentc_test` 钉它是内建表的子集）。判据 = 拿参考实现**真正的词法器**独立数一遍，语料是**全部 32 份示例**；另配词法夹具 `loment/lex/boundary.lomt`（32 份里 `extern fn` 一次都没出现，那一族本来是空跑的），五个数**写死**；变异测试过。<br>**② 计数进了 Potato**（`v7` 的 `boundary`，形状见 `docs/147` §5）。**这一半把 `v6` 一起带上了**：加字段要升 v7，而 v7 的台阶累积、会让 v6 的 `grammar` 一起必填 —— 可主编译器**从 v6 起就没产出过 `grammar`**（那一档的产出方是 `potato_from`），一升就撞上编译器的**自检**。`lomentc` 其实读得到那个声明（`load()` 走 `front_door`，`FrontUnit.grammar` 就在手上），所以接上了 —— 于是这次顺带把 **v6 接进了主编译器**。**记一笔**：这不是"顺手改别的线"，是台阶累积本身逼出来的，而且**漏接一版编译器自己的自检当场就红**。<br>**判据**：`loment_potato_emit_test` —— **42 份单元**的形式对象与参考实现**逐字节相同**（两个实现一个是 Python 走 AST、一个是自举走 token 扫描，两条路互不相干）；`potato_test` **105 份对象**（含 6 条 `boundary` 反例）两份校验器**判决与错误条数逐条一致** | 一轮（已完成） |
| R6 | Cargo 与统一工具链（**doc-test** 那一半） | **抄** | 包内的示例**每一个都进判据**（编译 + 跑 + 比 stdout）；`docs/manual` 里出现的代码块由**机械抽取**参与判据 —— 文档里的例子不能静默腐烂。<br>**进度（2026-09-23）：已落地。** ①运行判据：`loment_elf_test` 的语料从 4 份放宽到**能跑的每一份**（8 份），另 24 份进 `NOT_RUN`、**每份带一个当场能核的理由**（判据真去链一次 / 真去编一次），`test_every_example_is_decided` 钉"32 份被两张表划分完"。②放宽语料**当场抓到两个真缺口**：一份是容量上限（点名拒），一份是**崩** —— 自举镜像在模块级 `i64` 全局上 SIGILL，已带受控复现提成 **issue #102**；两者记进 `MIRROR_GAP`，表**只许变短**。③文档代码块抽取从"只扫 SKILL.md"放宽到 SKILL/README/QUICKSTART 三份（17 块）；`docs/manual` 的示例页**就是** `loment/examples/*.lomt` 生成的，那一半由①②覆盖。④顺手钉住 `README` / `QUICKSTART` 里那个"N example programs" —— 此前 README 写 30、QUICKSTART 写 28、盘上是 32，**没有任何东西看着**。⚠ 原判据里的"30 个示例"是错的 | 一格 |
| R7 | 卫生宏 | **明确不抄** | 理由：宏的代价里"**诊断质量下降**"与本仓的"每条诊断都有码与修法"（`docs/182`）直接冲突。语法糖改由**六门表层语法 + `comefor` 方言**承担（`docs/188`）。**它的判据是"这条冲突被写下来"** | 不抄 |

### 1.3 Java（7 条）

| # | 优点（`docs/204` §3） | 裁决 | 0.1.5 Alpha1 的判据（可证伪） | 量级 |
|---|---|---|---|---|
| J1 | 字节码 + VM 解耦 | **换形式抄**（要"语言可换"，不要 VM） | 等价物 = **语义固定、表层可换**：六门表层语法 + `choose write grammar`（`docs/188`/`docs/179`）。判据已有；0.1.5 补：**六门各一个大项目**在判据里 | 一格 |
| J2 | JIT（运行时画像） | **明确不抄** | 与"无运行时 / 确定性"冲突；真要做性能的那条路是 **AOT + PGO**，不是 JIT。这条冲突写下来即判据 | 不抄 |
| J3 | 成熟的 GC | **换形式抄**（形态已定） | `docs/175` §3.4 那五条：进 Potato / 每维一次 / 默认不变 / **`free` 不再占位** / `no_std`+`gc_auto` 拒。`gc_auto` 若要，按"受约束的子集"开，不放进核。<br>**进度（2026-09-23）**：**这一维已进语言** —— `choose gc_manual` / `choose gc_auto`，两个实现逐字节一致，Potato **v8**（`gc` 字段，**必填**，所以"这个产物放弃了确定性没有"是不读源码可判的）。五条里前四条有判据（进 Potato / **按维**一次 / 库不许 / `no_std`+`gc_auto` **点名拒**），第五条"默认不变"由 `loment_potato_emit_test` 的逐字节孪生守着。<br>**两格还没做**，各有判据：① `gc_manual` 要把 **`free` 兑现**（今天它是占位，`alloc` 是 64 KiB bump 堆、不归还）—— 一段"总量超过 arena 但峰值不超"的程序今天会耗尽；② `gc_auto` 的**收集器**。② 的一条**更正**：原判"要 L0"（`docs/176`），复核后是——**问 OS 要页**才要 L0，而一个 **arena 内**的收集器不要<br>**进度（2026-09-24）**：那条**受约束的子集**开了第一个格 —— `gc_auto_alpha` 的 **L0（静态提升）**在**两个实现**上落地、逐字节一致，四条判据（`lomentc_test` 三条 + `loment_p8_test` 一条）；形态与价钱见 `docs/210` §2。**这一格顺带兑现了一件事**：一份全 L0 的 alpha 产物里**连分配器都不剩**（`@__loment_alloc` 一次都不出现 ⇒ 那 6 KB 运行期自己掉出去）。**再进一格（同日）**：**L2（块纪元）**也落地了 —— 循环体末尾把分配器前沿退回去，整段一次性不存在（"回收时间而不是对象"），**零新语法**：纪元边界由编译器从块结构 + 逃逸**推出来**，不是写出来的（`docs/210` §2.3）。动态判据实测：同一份程序默认档**耗尽**（rc=132）、`gc_auto_alpha` **跑完**（rc=7，4 万圈 × 64 B ≈ 2.56 MB 跑在 64 KiB arena 里）。**再进两格（2026-09-25）**：**L3（`gc_auto` 的收集器）**落地 —— 标记-清扫 + 精确根 + 按分配量触发，两个实现逐字节一致，三条判据（静态痕迹 / 字节一致 / 动态一对）；有一条明写的上限：触发 32 KiB 而 arena 64 KiB，所以**活集 ≳ 32 KB 仍会耗尽**（`docs/210` §2.1 末尾）。**L1（定活 + 插 `free`）**也落地了 —— 最后一次用处之后立刻还，**零新语法**；静态真值表 12 格 + 字节一致 + 动态一对（4000 次调用 ≈ 288 KB 过 64 KiB arena：alpha 跑通 / 默认档耗尽）。**四层梯（L0/L1/L2/L3）到此都有实现、都有判据**；剩下的只有 L3 的残差面（分代 / 自适应），见 `docs/210` §2.4 没抄的那几条 | 多轮（四格已落地） |
| J4 | 名义类型 + 显式接口 | **抄** | `trait`/`impl` 按名（M8 已有）+ 能力域是名义契约（M35–M43 已有） | 已有 |
| J5 | 向后兼容当最高纪律 | **抄**（形式 = 冻结面 + **edition**） | 冻结面四条流程已有（`docs/158` §5）；0.1.5 要定的是 **edition 机制**：改名/改语义的新语法与旧语法**并存**，判据 = `loment_rule_parity` 的 `BUDGET` 棘轮只升不降，且旧语法仍有一族绿判据 | 一轮 |
| J6 | 内存模型被规范（JMM） | **抄意图** | 成文写清"什么保证 / 什么不保证"（原子、调用约定、trap 与 UB 的界线），并有一条判据钉住它的发射（如 `atomicrmw … seq_cst`）。**今天这条是空的** | 一格 |
| J7 | 受检异常（**有争议**的优点） | **换形式抄** | 要**意图**（错误不可忽略）：`Result` + 穷尽 match + `lomenterr` 的"修法"；**不要形式**（checked exception 的语法噪声） | 已有 |

### 1.4 Python（8 条）

| # | 优点（`docs/204` §4） | 裁决 | 0.1.5 Alpha1 的判据（可证伪） | 量级 |
|---|---|---|---|---|
| P1 | 可读性当第一目标 | **换形式抄** | 等价物 = **一种显然的写法**：`loment fmt` 与 Python 版逐字节相同（C6/C12 已有）+ `loment cheat` 的每一条都在判据里 | 已有（+ 钉紧） |
| P2 | **REPL 与交互式** | **换形式抄** | 解释器**已经在**（`loment interp`）→ 做成 `loment repl`：会话里 `use` 一个模块、调一个函数、打印结果，stdout 逐字节比对；**不引入**半成品 JIT | 一格 |
| P3 | 动态类型 / duck typing | **明确不抄** | 与"编译期能回答碰什么"（C6/J4/能力域）直接冲突 —— 我们选静态那一边。冲突写下来即判据 | 不抄 |
| P4 | 内建数据结构与切片 | **抄** | 切片/数组（M3/M4 已有）+ `std` 127 模块的行为判据（`loment_std_test` 已有） | 已有 |
| P5 | 一切皆对象 + 内省 | **换形式抄**（形式 = Potato，不是运行期反射） | "程序能回答关于自己的问题"由**形式对象**承担，且**两个独立校验器**逐条判（C1/`potato_test` 已有）；增量：Potato 的公开读取接口要有判据 | 已有（+ 钉紧） |
| P6 | 生成器 / 迭代器协议 | **明确不抄**（语言级惰性） | 理由：语言级惰性序列要么分配、要么把状态机写进类型 —— 前者撞"无运行时"，后者撞 R1 的边界。**抄它的形状**：`for … in` 已经能用切片/数组 | 不抄 |
| P7 | `with` 与上下文管理器 | **换形式抄**（形式 = 作用域 + Drop） | 词法作用域结束即释放（M16 `Drop` 已有，`native_raii.lomt` 在判据里）；增量：释放**真**还内存（= J3 的 `free`） | 已有 + J3 |
| P8 | 标准库 + 生态广度 | **抄广度，不抄中心注册表** | 库 = 目录、身份 = 内容哈希、store = 文件；**没有中心索引**（`lompi`/`loment_lib_test` 已有）。增量：把"没有中心"的**代价**写成文并接受它（可发现性差） | 已有（+ 文档） |

---

## 2. `docs/204` §5 那六条对照，落到我们头上

那一节比四张表更值得读，因为**它才是真正的分歧**。每一条我们都已经选了边 ——
0.1.5 Alpha1 要做的不是重选，而是**把选的那边写进判据**：

| 对照 | 我们的立场 | 判据 |
|---|---|---|
| **可移植性** | **编译期**（同一份 IR → ELF / PE / 裸机对象），不插 VM；代价是自己写后端 | C19 / C20 / C24 三条主张 |
| **成本在哪一刻付** | **混合，且要写清分界**：静态能证的（移动、借用子集、类型、能力域字面量）在编译期；证不了的（非字面量 `guard`、越界）**运行期 trap** —— **不许是 UB** | J6 那条成文 + M36 |
| **"这段碰不到什么"由谁回答** | **类型（能力域）+ 形式对象（Potato）** —— 让它可以被**机械回答**，而不是靠约定 | C1 / `potato_test` / `docs/146` |
| **依赖与模块** | **文件即注册表**：`use` 即依赖、库 = 目录、身份 = 内容哈希 | `lompi` / `loment_lib_test` |
| **版本演进** | 冻结面（`docs/158`）+ **edition**（J5，0.1.5 要定） | `BUDGET` 棘轮只升不降 |
| **可预测性 vs 峰值性能** | **可预测**。相应地：**不主张性能数字**，只报自编译时间 | 无（这是"不作主张"，不是判据） |

---

## 3. 明确不抄的（连同理由，一次性列出）

| 不抄 | 撞的是哪条冲突 |
|---|---|
| **C 的 UB**（`docs/204` C1/C5 的**代价**那一格） | 越界要 **trap**，不是 UB。这是本仓最不肯让的一格（`docs/188` 的"宁拒勿猜"） |
| **Rust 的 NLL 级借用检查** | 要把生命周期放进类型；代价是表达力与曲线。我们用**能力域 + 移动/借用子集**覆盖，并把边界成文（R1） |
| **Rust 的宏** | 诊断质量（R7） |
| **Java 的 JIT** | 无运行时 / 确定性（J2） |
| **Python 的动态类型** | "编译期能回答碰什么"（P3） |
| **语言级的惰性序列** | 分配或状态机进类型，两条都撞（P6） |
| **中心化的包索引** | 与"文件即注册表"冲突（P8）；代价（可发现性）我们认 |

**这一节的作用**：0.1.5 Alpha1 说"完成优点集合"时，**这七条是排除在外的**，
而且**排除本身有理由** —— 不是漏做。

---

## 4. 0.1.5 Alpha1 的完成判据

1. **零沉默**：§1 那 28 行**每一行**都有裁决（抄 / 换形式抄 / 明确不抄）与一条判据；
   出现"未定 / 待议 / 视情况"即未完成。
2. **判据能跑**：每条判据要么已经在门禁里，要么进 `tools/ci.py` 的静态门禁并
   **不在 `.github/gate-baseline.txt` 的基线里**（基线只许变短）。
3. **`docs/202` 的三项未完成不另立门户** —— 它们**就是** §1 里若干行的判据：
   第 3 项（hosted 层）对应"运行期 trap 与 UB 的分界"这条线，第 4 项（GC 两档）就是 J3，
   第 5 项（完全自举）是 R4 + C1。**它们完成 = 这张表的一部分完成。**
4. **版本与出包**：`tools/loment_release.RELEASE` 进到 `0.1.5-alpha1`；按 `docs/162`
   出包（tag + 四件资产），且**发之前两条安装路都真装真跑过**（`docs/202` 第 8 项
   那次的做法成为常规）。

---

## 5. 我不主张

1. **这不是排期。** 量级那一列只分四档，**工期一个字都不猜**（与 `docs/202` §4 同一口径）。
2. **不主张这张表完备。** 它是 `docs/204` 的镜像；204 里没写的优点，这里也不会出现。
3. **不主张它是承诺。** 它是一份**账**：能让"0.1.5 Alpha1 做完了没有"变成可证伪的问题，
   而不是感觉。
4. **不主张 §1 的裁决都对。** 每条裁决都该被挑战 —— 挑战的方式是**指到它的代价那一格**，
   而不是说"我觉得该抄"。

## 6. 与其它文档的关系

* `docs/204` —— 输入（优点收集，机制→得到→代价）。
* `docs/175` §6 —— 方向账与取舍的入口；**本文是它在 0.1.5 这一档的镜像**。
* `docs/202` —— 0.1.4 的发布前置账；它未完成的三项**落进本文 §1 的若干行**。
* `docs/158` —— 冻结面与改动流程；本文里标"一轮"的每一行都要走它那四条。
* `docs/146` / `docs/147` —— 能力域与 Potato 的语义（C6/P5/R5 的落点）。
