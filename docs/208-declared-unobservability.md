# 208 · 第二层：为什么它在这门语言里**攻不下来**（以及该往哪拆）

> ⚠ **更正（2026-09-23，第二次复核）—— 本文原来的主**张**有类型错误，而且不止是类型错误。**
>
> 并行会话指出 §2 那三行里 **`x` 左边当"值"、右边当"空间"**（`Obs*(u)` 是**空间的集合**）。
> 核过原文，确认。**而往下一查，它不只是写错了一个符号：**
>
> **能力空间是"外部"那一侧的声明（net、disk……），而 `alloc` 返回的 arena 内存
> 不属于任何能力空间；一个堆块是被**指针**观察到的，不是被空间观察到的。**
> ⇒ **`Obs*` 是一份关于"外部交互"的判据，它落不到堆上。**
> ⇒ 所以本文原来的主张（"声明上的不可观察 ⇒ 可回收，因而可判定"）**不成立**：
> 我把一条**保密／信息流**的声明（`excluded` 回答"我不会把数据送出去"）
> 当成了**回收**判据（"这块能不能丢"）—— **两个不同的问题。**
>
> **能救回来的那条（是真的，而且指向正确的东西）**：Loment 里**真的**有一条编译期可判、
> 且**关于堆**的判据 —— `docs/206` 那套**移动／借用子集**（E006/E007/E012），也就是
> `docs/205` R1 说的"**编译期就能回答'这段碰得到什么'**"。**它才是这一层该用的杠杆。**
> 它的界是 `docs/206` §2 那四条"不证"。⇒ §3／§4 那些关于**空间**的东西（`closed`、静态调用图）
> **是真的，但对回收没有用** —— 它们该去的地方是**信息流**，不是 GC。
>
> **根因见 §2.2**：那不是"缺口待补"，是**箭头反了** ——
> 信息流是**外向许可**，回收要的是**内向可达**。下一格（一个**可量的数**）见 §2.3。
>
> 审核与推理过程见 §2.1。

> 状态: 探索稿（2026-09-23）· 承接 `docs/207`（**其 §1–§2 已被前史覆盖，见该文顶部的撤回块**）
> · **修正后本文的主张只剩"该往哪拆"** · 不改 L0。

## 0. 一句话

> **判据不是能力的「存在」，是能力的「不可达」。**

## 1. 为什么第二层在这门语言里攻得下来

传统 GC 的判据阶梯（`docs/207` §1）第 2 层是"**语义上未被观察**"，
一般不可判定 —— 因为它量化的是**程序的未来行为**（"以后还会不会读它"）。

但 Loment 有一件别的语言没有的东西：**"能观察到什么"是声明的，不是推断的。**

| 声明装置 | 它说什么 | 判据 |
|---|---|---|
| `capability N : space[lo..hi]` | 这个单元**能观察**哪些外部状态 | 能力表（编译期） |
| `excluded "space: ..."` | 这个单元**不申请**某空间（负向） | **M41 / E004**（`docs/146`、`docs/145`、`docs/150` 的"第一遍半"） |
| **Potato** | 把上面的声明记进产物 | **不读源码可判** |

于是命题可以收紧成一句**可判定**的话（这就是 peer 那份转达里的主命题）：

> **不可观察集 = 声明上不可观察的集。**
> 不必判"有没有被观察"，只判"**在不在一个声明为不可观察的域里**"。

这比"可达"强（它是语义的、不是指针的），比"语义未观察"**可判**（它不量化未来，只读声明），
**而判据是现成的**（M41）。

## 2. 精确形式（比"声明上不可观察"还要紧一格）

令 `Obs(u)` = 单元 `u` 声明覆盖的空间 \ `excluded` 声明的空间。
`Obs(u)` 是**局部**的、静态的、M41 已强制的。

但回收判据需要的不是它，是**闭包**：

```
Obs*(u)  ≔  Obs(u) 沿「u 能调到谁」取传递闭包
（原文此行写作"`x 可回收 ⟺ ∀u : x ∉ Obs*(u)`" —— **那是类型错误**：
 左边的 `x` 是**值**、右边的 `x` 落在**空间的集合**里。**这一侧只说得出"空间"**；
 堆那一侧要另加一层，而那一层**不成立** —— 见 §2.1。）
```

**关键差别**：

> **M41 保证的是 `Obs(u)` 局部自洽，不保证 `Obs*(u) = Obs(u)`。**

`excluded "net"` 说的是"本单元不申请网络能力"——**它不说"本单元到不了网络"**。
只要 `u` 能调到一个有网络能力的 `w`，`u` 就能**间接**观察网络。
`Obs*(u)` 里就多出了 `Obs(u)` 里没有的东西。

### 2.1 缺的那一步，以及把它走一遍的结果（2026-09-23）

**缺的那一步**（并行会话点名的）：从"空间"到"堆块" ——
"**这个块的字节能不能流进 `u` 可达的某个空间**"，一层**流向分析**，不是一条声明。

它提了一个可验的猜想：这层分析也许**一石二鸟** ——
"流不进任何可达空间" ≈ "纯内部" ≈ **可重算的候选**；若两者同源，就"只花一次工"。

**把 `§7` 那两个最小反例放进去走一遍，结果是：它落在 (a) 也不落在 (b)：**

* **反例一（同一次调用内两次 `&mut`）**：`p` 从不流向任何空间 ⇒ 流向分析会说"外部不可观察"
  ⇒ 判它**可丢**。**而程序正在用 `*a` / `*b` 读它。**
* **反例二（单调 bump）**：同样 —— 那些块不流向任何空间，而程序在读它们。

⇒ **两个反例上它都给出"可丢"，而两次都是错的。**
⇒ 所以它不是 (a)「丢了就再也不需要」，也不是 (b)「丢了以后重算」，**它是 unsound。**

⇒ 根因在 §2 那个类型错误的下游：**堆块是被指针观察的，不是被空间观察的**；
"流不进外部空间"和"没人读它"**是两件事**。⇒ 这层分析**接不到回收上 —— 它接了个名字。**

### 2.2 根因：**箭头是反的**（并行会话把它说死了）

比"缺口待补"更准的一条结论：

* `Obs*` 回答的是 —— **这个单元能把数据送到哪些空间去**（**外向**的许可）；
* 回收需要的是 —— **哪些指针还能摸到这块内存**（**内向**的可达）。

**两个方向在同一条图上相反。** `excluded "net"` 是"我**不往外送**"，
它**推不出**"没人**指向我**"。

⇒ 所以 §2.1 那个实证**不是"分析太弱"，是分析指向了另一边**。
⇒ **`closed` 无论做到多传递，都不可能是回收判据** —— 不是强度不够，是**它不回答那个问题**。
⇒ 也不是"再补一层流向分析"能救的：要判那块可丢，你得知道**内部指针的活跃性**，
**那就是可达性本身** —— 兜回来了。**信息流分析不是可达性的替代品。**

> **一支指错方向的箭，不是一道待补的缺口。**

### 2.3 救回来的那条，连同它的**强度**（下一格是个可量的数）

**堆那一侧的杠杆是 `docs/206` 那套移动／借用子集（E006/E007/E012），不是能力（`docs/142`/`146`）。**
这与 `docs/206` §0 早就写着的"R1 换形式抄 —— 要那个**目的**，不要那个**形式**"是同一件事。

**但强度要一起写**：`docs/206` 证的三条**很窄**（E007 只在**同一次调用之内**、不看生命周期、
不跨函数、无字段级移动、移动集不分分支），而 **§7 的第一个反例（两条语句各取一次 `&mut`）
正好落在那个边界上**。

⇒ 所以下一格不是"能不能拿到单射"（§7 已答掉），而是：

> **`docs/206` 那个子集，实际能买到多少堆上的活跃期？**

**这是个可量的数**（形状与 §9 那三个一样）：拿语料统计"指针的活跃期能否被
E006/E007/E012 判到边界"，得到一个**覆盖比例**。
**这个数才决定这条线有没有下一步** —— 而且它不碰冻结面、不用新关键字。

## 3. 缺口：传递（**这是全文的关键**）

> ⚠ **本节（以及 §4）已被 §2.2 取代为结论。** 它们都在**空间**那一侧 ——
> **成立，但对回收没有用**：那支箭指向的是**信息流**，不是**可达性**。
> 保留在这里是因为"传递缺口"是我当初的入口，而 §2.2 说明了它为什么不是缺口。

缺口就在上面那一条。两条出路，一条能落、一条是真话但窄：

### 3.1 `closed "space"` —— 把 `excluded` 从「我不申请」升级成「我到不了」

`closed "S"` 的语义应当是：

> **本单元及其可达闭包里，不存在通往 `S` 的能力。**

它的判据面从**能力表**扩到**调用图闭包**，与 M41 **同族**（`docs/146` 那张表加一行的事），
形状大概是：

```
单元 u 声明 closed "net"
  ⇒ 对 u 可达闭包里的每个 w：w 的能力表 ∩ net 空间 = ∅
  ⇒ 否则编译错误（M41 的传递版）
```

代价：`closed` 比 `excluded` **强**，所以它**更难声明** —— 它把一个单元和它调用的一切绑在一起。
一个库改了依赖，可能让上游的 `closed` 失效。这是它的价格，不是它的缺陷。

### 3.2 或者：**只对叶子单元成立**（今天就能立）

叶子单元（不调用别的单元）没有传递问题 —— `Obs*(u) = Obs(u)`，判据就是 M41。

**这是真话，而且今天就可判。** 覆盖面窄，但它不需要任何新关键字、新判据。
写在这里当作"这一层今天能落地到哪儿"的答案。

### 3.3 缺口今天**咬不到任何地方**（2026-09-23 量过）

并行会话用 `lomc.lex`（与 `loment_cli_test` 同一把尺子）量了：

| 语料 | 合法单元 | 叶子 | 引别的单元 | 其中**声明了能力**的 |
|---|---:|---:|---:|---:|
| `examples` | 55（+12 词法不过） | **76.4%** | 13 | **1** |
| `lib` | 10（+1） | 40.0% | 6 | **0** |
| `tools` | 29 | **13.8%** | 25 | **0** |
| `selfhost` | 42 | 73.8% | 11 | **0** |

我另做了一个**上界校验**：语料里声明 `capability` 的**文件** —— examples 3 / selfhost 2 /
**lib 0 / tools 0** ⇒ 候选池 ≤ 5，而两处交集量到 **1**。

⇒ **"既声明能力、又调别的单元"的单元，全语料 1 份。** 于是：

> **§3 那道传递缺口今天咬不到任何地方。** `closed` 会被造出来服务**零个现有用户**。

这不否定它，但**顺序该反过来：先有一个用例，再有关键字** —— 否则是空转。

## 4. 这一族的门：**静态可调用图**

> ⚠ 同上：这门是**关于空间**的（"`u` 能调到谁"决定可达闭包的形状）。
> **它成立**，但**它服务的是信息流，不是回收。**

上面两条路（以及 `docs/207` §11 提到的"静态 frontier 表"）都要**同一个语言性质**：

> **`u` 能调到谁，是编译期可知的。**

于是它从"某个方案的前提"升级成**这一族方案的门**。在 Loment 里，这个门的具体问法是：

* 动态分派（trait 的多态调用）算不算"能调到谁"的边？
* 函数指针 / 间接跳转呢？
* `choose` 已经有一维 `runtime`；这里要的是一个**图的形状**，不是开关。

**这道门比 `closed` 本身重要**：`closed` 只是个声明，门是"这张图存不存在"。
如果图是静态的，`closed` 就是 M41 的一次推广；如果不是，这一整族东西都只能退到叶子单元那一档。

> ✅ **门是开的（2026-09-23 由并行会话查到，我逐条复过）。**
> * **静态派发**：`tools/lomentc.py:725`"给 MethodCall 打上具体函数名（**静态派发**）：
>   `obj.m(...)` -> `Type_m(obj, ...)`"、`:1384`"名字按接收者类型混淆"；自举侧同形
>   —— `loment/selfhost/codegen.lomt:861 emit_method_name`。
> * **间接调用不在支持面内**：`tools/lomelf.py:25` 明列"**没有**：……**间接调用**、闭包……"，
>   `:1009` `raise Unsupported("v0 不支持间接调用")`。
>
> ⇒ **"`u` 能调到谁"在这个语言里是编译期可知的。** 所以 `closed` 是 **M41 的一次推广**，
> 不是研究题 —— **§4 这道门从"待查"改成"已开"。**
> （§9 第三个数也随之定了：动态分派占比在 Loment 里**恒为 0** —— 它不是"多少"，是"**没有**"。）

## 5. 它证不到哪儿（诚实边界，别当成已经解决了）

1. **间接可达**（§3 那个缺口）—— 非叶子单元上，`excluded` 不足以支撑判据。
2. **动态分派 / 间接跳转** —— 会让"可达闭包"从静态表变成不可知；**但今天这两样都不在语言里**
   （§4 的 ✅ 已查：门是开的）。这条是"**将来加了会怎样**"，不是今天的缺口。
3. **`excluded` 的语义今天不是这个** —— 今天它是"**不申请**"，不是"**到不了**"。
   把它读成后者是**误读**，本文的全部主张都建立在"要给它加一个传递版本"之上。
4. **即便 `Obs*` 全部成立，它给的是上界，不是精确值**（§6）——
   它是"**最多**能观察到这些"，不是"**恰好**观察到这些"。
5. **`guard` 不是这条链的一部分。** `guard CAP(e)` 是**审计点**（`@__loment_audit`，`docs/147` A2），
   它记的是"发生过一次域检查"，**不是**"这个单元能观察什么"。别把两者混起来。

## 6. 与物理目标的关系（预告 `docs/209`）—— 以及这条类比**在哪儿断**

我最初把它写成"观察集 = 率失真理论里的失真度量，回收 = `R(0)`"。
**一条来自并行会话的纠正说明这个类比只在半路上成立**，记在这里，因为它改变了 `docs/209` 的脊。

**对的那半**：

* 把**合法观察集**当作信息论里那个**失真度量**（观察不到的区别 = 零失真），
  回收就是**零失真约束下的率最小化**：`R(0)`；
* 拿**恒等**失真（每个比特都能被观察）⇒ `R(0) = H(X)`，一个比特都压不掉；
  拿**声明的观察集** ⇒ 失真度量变粗 ⇒ 地板降低；
* 而"最短描述"版本就是 **Kolmogorov 复杂度** ⇒ 一般**不可判定**。
  ⇒ **第 2 层那堵墙与物理那堵墙是同一堵墙。**

**断掉的那半（这条才是要紧的）**：

> **`R(0)` 只是"商集的熵"** —— 一个**单点**，不是曲线。它给"不可约信息"一个标准名字，
> **但没有带来任何定理**：`D = 0` 意味着"答案不许错"，率失真那套**权衡**结构整个塌掉，
> 剩下的话与 Baker 1992 / Wu 2026 说的是同一件事。

**率失真真正的内容在 `D > 0`** —— 而 `D > 0` 在这门语言里意味着**允许算错**。
所以这条类比要成立，语言得先有一样东西：**声明的误差预算。**

好的那面是：这个"声明"与本文 §2 的"声明"**是同一套装置**
（`capability` / `excluded` / `closed` 那一片就是声明面）：

| 声明什么 | 得到什么 | 有定理吗 |
|---|---|---|
| **我能观察到什么** | `R(0)` 这个地板（= 不可约信息） | **没有新定理**（就是 Wu 2026 的范围） |
| **我能错多少** | `D > 0` 的权衡曲线；`Obs*` 这时才真是可算上界 | **有** —— 率失真的正典在这里 |

> **两条轴不是"缝在率失真上"，是缝在同一张声明面上** —— 而只有后一半有定理兜底。

`docs/209` §6 展开后一半。它的前史是 **approximate storage / approximate memory**
（那里失真**是符号上的真度量**）与 **bit-rate 约束下的状态估计**
（Nair–Evans 那一系：率 = 允许的比特，失真 = 估计误差，源 = 一个动态系统）。

### 6.1 它与 Wu 2026 是**不是**同一个量（2026-09-23 读了原文）

并行会话提出：既然给定的输入下程序是确定的，那个熵就是输入的熵，那它就是 Wu 2026 在算的账 ——
**那么这半节该写成引理、引 Wu，而不是当自己的结果。** 我把 Wu 的原文抓下来抽了文本，结论是：

> ~~**不是同一个量。**~~ ⇒ **改口（第二遍读原文之后）：是互补的。**

**我第一次答错了，而且错在没读 Wu 的 §2 就下结论。** Wu 的定义（原文）：

* **Def. 3（Garbage process）**：`G_{1:t}` 是"**为让可见更新可逆而保留下来的 side records**"；
* **Def. 4**：`r_G = lim inf t⁻¹ H(G_{1:t} | R_t)`（率）；正文另给**同一个量的存量形**
  `B_G(t) = H(G_{1:t} | R_t)` ⇒ **Wu 的"率"与"存量"是同一个量带不带 1/t** ——
  所以我先前那句"率 vs 存量是差异"**不成立**；
* **Prop. 1**：若更新在边界内**单射**，则 `(Y, G_A)` 唯一确定原像 ⇒ **`H(X | Y, G_A) = 0`**；
  证明里直接写着 **`M_used(t) = M_0 + H(G_{1:t} | R_t)`**。

把 Prop. 1 代进链式法则：

```
H(状态) = H(X) = H(Y)   +   H(G | Y)
                 └─R(0)─┘    └ garbage load ┘
```

⇒ **互补，不是独立。** 我的 `R(0)`（可观察投影的信息）**就是 Wu 那本账的另一栏**。
⇒ 并行会话那句"**若互补成立，差值就不是结果，是分账**"因此**成立** ——
**本文的 `R(0)` 不是新量。** 它给的检验也是对的：要证独立得拿两个**存量**去比；
我先前拿率去比存量，**两端都拿错了**。

**但互补本身给出一句能用的：**

> **声明观察集 = 决定那一刀切在哪。**
> 观察集越窄，"**必须留**"那一栏越小、"**可以清**"那一栏越大。

⇒ 所以这一节能主张的仍然只有**那一处实例化**（把"程序声明的合法观察"当失真度量），
但它的**位置**变了：**它不提供新量，它提供切法。** 而切法正是 `Obs*`（§2–§4）能算出来的东西 ——
**可判定性那条轴在这里才真正接上物理**：`Obs*` 切的就是这本账。

## 7. 两条最小反例（"不要生命周期，能不能拿单射"）

本文的动机之一是 `docs/207` §5 那个研究问题。两条路各给一个最小反例，
都能从本仓**已写下的记录**推出来：

**路 (i)：在受约束子集里要求唯一引用 —— 拿不到单射。** 反例直接来自 `docs/206` §2.1
（E007 只在"**同一次调用之内**"查）：

```rust
let a = &mut p;   // 同一次调用内查过 —— 通过
let b = &mut p;   // 换一条语句，不再查 —— 也通过（docs/206 §2.1）
*a = 1; *b = 2;   // 写不是单射：*a 的旧值取不回来 ⇒ 反算不成立
```

⇒ **没有生命周期，就没有"唯一引用"；没有唯一引用，写就不是单射。** 这就是答案。

**路 (ii)："不可反算处退回收税" —— 在从不复用的程序上等于零内容。** 反例：单调 bump、
从不覆盖的程序。那里**没有擦除事件**，也就**没有税可省**（`docs/209` §3）——
方案退化成"什么都不做"。⇒ (ii) 不是通往单射的路，是**逃生阀**，
且它的适用面恰好落在"本来就不需要回收"的那类程序上。

## 8. 与 `docs/205` P6 的边界（能划清）

`docs/205` 的 P6 行把"**语言级惰性**"列在**明确不抄**，理由是要么分配、要么把状态机写进类型。
本文（及其物理侧）要的是"**物化可以退化成重放**"——**哪个物理副本留着**，是**回收语义**，
不是**求值语义**（值什么时候算）。

不撞的判据只有一条，而且很干净：

> **重放对程序不可观察**（结果不变；代价只落在时间上）。

一旦重放会改变可观察行为（失败 / 顺序 / 副作用），它就变成求值语义 ⇒ **撞 P6**。
**而这条判据与 `docs/207` §10 的度量 3（正跑 → 反跑 → 逐位比对）是同一条。**

> ### 一处判据，三扇门
>
> 上面这条"**重放对程序不可观察**"同时是：
>
> ① **安全义务** —— 逆不精确就不反算，退回普通回收，**永不出错**；
> ② **不撞 P6 的判据** —— 一旦重放改变可观察行为，它就从"回收语义"变成"求值语义"；
> ③ **度量 3 本身** —— `docs/207` §10 那条正跑 → 反跑 → 逐位比对。
>
> **一条判据同时关三扇门。** 这是这批文档里最该单独拎出来的一处（并行会话点名要求）。

## 9. 判据（前三个已量；**第四个才是决定这条线有没有下一步的**）

| 要量什么 | 结果 | 出处 |
|---|---|---|
| **叶子单元占比** | examples 76.4% / selfhost 73.8% / lib 40.0% / tools 13.8% | 并行会话（`lomc.lex`） |
| **既声明能力、又调用别的单元的单元数** | **1**（候选池 ≤5） | 并行会话量 + 我的上界校验 |
| **可调用图的静态度** | **恒为 0** —— 不是"多少"，是"**没有**" | §4 的 ✅（`lomentc.py:725` / `lomelf.py:1009`） |
| **`docs/206` 子集买到的堆上活跃期覆盖比例** | **未测** —— 拿语料统计"指针的活跃期能否被 E006/E007/E012 判到边界" | §2.3 |

**读法（更正后）**：前三个数说的是**空间侧那套东西**在语料里的分布 ——
它们记录了代价，但按 §2.2 **对回收没有用**（箭指错了方向）。
**第四个才是这一格的真问题**，而它还没量。

## 10. 前史

* **`docs/206`** —— 所有权那条边界（证的 E007/E006/E012、不证的四条）。§7 的反例直接从它来。
* **`docs/146` / `docs/145` M41 / `docs/150`** —— `excluded` 的现行语义与它今天的判据（**局部**）。
* **`docs/142`** —— `excluded` 的负向语义原文。
* **semantic rate-distortion**（通信/信息论，如 *An Indirect Rate-Distortion Characterization
  for Semantic Sources*, arXiv:2201.12477）—— **"失真度量落在语义上而不是比特上"这个想法已经存在**。
  本文 §6 与它的区别只有一处：**这里的失真度量是「程序声明的合法观察」**，
  不是统计意义上的语义相似度，且**由编译器强制**（M41）。**这一处区别我没有找到直接的先行工作 ——
  但按 `docs/207` 的教训，这句话在没查干净之前只能当「未证实」。**
* **`docs/207`** —— 那条探索线；其 §1–§2 已撞 **Baker 1992**（见该文顶部）。

## 11. 没做的（写明）

* **`closed` 没有实现、没有判据。** `docs/146` 那张表加一行是**提案**，不是已完成的事。
* **§9 三个数一个都没量。**
* **可调用图的静态度没查** —— 而它是 §4 那道门本身。`lomentc.py` 今天对动态分派怎么处理，
  本文没有去读。
* **叶子单元那一档没有实测覆盖面** —— 若叶子很少，§3.2 就只是"原则上成立"。
* **没写英文版。**
