# 211 · C 表层语法：一轮实测扫面（38 条 issue）

> 2026-09-25。做法是**先用 C 拼法写一个纯 Loment 项目**，写不动的地方就是语料。
> 项目落在 `loment/examples/cproject/`（宿主原生 Loment + 两个 C 拼法叶子），
> 报告在这里，缺陷逐条挂 issue（#115–#155 里属于本轮的那 38 条）。
>
> 上游：`docs/188`（表层语法的显式声明）· `docs/186`（C→Loment 翻译，Stage A）·
> `docs/198`（上一轮从 LumtUI 报上来的四条）· `docs/182` §1.9（"读 L1 源的入口"）。
>
> **这份文件只上报，没有动语言线任何一份实现** —— `tools/ctrans.py` / `trans_core.py` /
> `potato_from.py` / `lomt_from.py` 一行没改（其中 `potato_from.py`、`lomt_from.py`
> 在本轮开工**之前**就带着另一个会话的未提交改动）。修法归属由语言线定。

---

## 0. 一句话

C 这一门能编能跑，但**它周围的每一层都在对同一件事给出不同的答案**：
同一份源，前门与 `lomt_from.py` 说法不同；同一份文件，`check` 与 `doc` 说法不同；
同一件事，报错指向的行号属于用户从没写过的那份文件。

38 条里，**按本仓自己的红线排**，最值钱的是"静默"那两类（编得过却算错 / 东西没了却不报），
共 **14 条**。

---

## 1. 怎么测的

**夹具**（都在 `.tmp-cwork/`，未入库）：一份 `.lomt` → `potato_from.front_door` →
`lomentc.load` → `resolve_deps` → `check` → `emit_llvm` → `lomelf.compile_ll` → 写 ELF →
在 WSL 里跑，读退出码与 stdout。

**对照面**：数值类一律与**一场独立的 C 编译**比 —— `clang -O1 -ffreestanding -nostdlib`
（`docs/186` §1 那条原判据）与 WSL `gcc -O0` 都用了。**不是手算、不是拿译文自己比自己**
（`docs/186` §1 那条"要与独立算出的期望值对一次"）。

**装的 `loment` 0.1.4 CLI 走不了这条路**：自举侧只认 `choose write grammar loment`，
非原生写法一律退 **1** 并说"请用参考实现"（`docs/189` §4.1 已声明的在制品）。
所以编译器这条路走 `tools/`；**自举侧相关的那两条**（`doc` 退 0 给错文档、LSP 死）
另用**仓内 `loment/build/dist` 的自举产物**复测过 —— 那份 `loment version` 报
`commit d80ab09`，就是 HEAD。

---

## 2. 项目：拆法不是风格，是**唯一能跑的那种**

```
loment/examples/cproject/
    main.lomt       宿主。原生 Loment：use、跨单元调用、类型转换、输出
    bitkit.lomt     叶子。C 拼法：位运算 / CRC-32 / varint
    numkit.lomt     叶子。C 拼法：数论 / 排序网络 / 控制流
```

跑出来 `bits=533 nums=917 total=1450`（退出码 170）。三个数都是在 Python 里照 C 语义
**另写一份**算出来对过的。

**为什么宿主不能也用 C 拼法**（四条一起，逐条实测）：

| 想在 C 拼法的单元里做 | 结果 |
|---|---|
| 被 `use` | **可以** —— 前门发出来的就是 `pub fn` |
| 自己写 `use` | **静默消失**（对象、`skipped` 都不留痕）→ **#121** |
| 调别的单元的函数 | 拒，报"本单元没有的函数"（Stage A 不跨单元）；`docs/186` §9 留的 `pub extern fn` 口子在 C 拼法里**没有写法** |
| 两种整型之间转换 | **做不到**：无隐式转换，`(T)x` 又在子集外 → **#125** |

⇒ **C 拼法的单元只能是叶子**。所有 `use`、跨单元调用、跨类型转换都得由原生那一层承担。
"每个单元都用 C 拼法"今天做不到 —— 这是这一轮关于形状的**结论**，不是抱怨。

---

## 3. 缺陷（38 条，按类别）

### 3.1 静默错：产物编得过、跑得动，**数不对**（7 条）

这一类按本仓红线最重（`docs/186` §4："少一个就是'这个程序少算了一步'，而它照样能编过"）。

| # | 一句话 | 对照面 |
|---|---|---|
| #118 | 函数内 `static` 被丢掉，值不跨调用保留 | clang 12 / 译文 11 |
| #119 | 循环体里同名声明 → 步进改到被遮蔽的名字上，**死循环** | gcc 27 / 译文不终止（timeout 124）|
| #120 | C 的**块作用域**没保住：`if`/`while` 体里的声明重绑外层 | clang 1 / 译文 2 |
| #123 | `int`/`unsigned` 混算原样照发，按有符号算 | gcc 7 / 译文 8 |
| #130 | 变量叫 `true`/`false`：声明改名、引用没改，读到的是**布尔字面量** | gcc 5 / 译文 1 |
| #131 | 非简单左操作数（负字面量、减法、移位）的比较**按无符号**做 —— `(-1) < 0` 为假，而同一单元的 `(-1) == -1` 为真 | gcc 1 / 译文 0 |
| #133 | `int` + `long` 混算，结果**取决于操作数顺序** | gcc 1 / 译文 0 |

另有两条宽度类（#132 超 32 位字面量静默截断、#124 `bool→int` 投影缺括号导致**译文解析不过**），
分类上属"①/⑤"，一并挂在那里。

**`docs/186` §6.3 那处论证**（"`static` 收下并丢掉是保义的"）只覆盖**文件作用域**的
`static` 函数；局部 `static` 说的是**静态存储期**，与链接无关 —— #118 就是它。

### 3.2 静默丢：东西没了，**谁都不说**（7 条）

| # | 丢了什么 |
|---|---|
| #122 | **同一行上的第二个函数**（`_C_FN` 锚 `^`；`skipped` 也空）|
| #126 | 顶层 `typedef` / 全局变量 / `union` / `#include` / `#define`（三条正则都扫不到 ⇒ 既不进产物也不进报告）|
| #129 | K&R 风格函数定义（连 `[skip]` 都没有）|
| #121 | C 拼法单元里的 `use` |
| #147 | 写在**语句位**的调用（值位会被拒，语句位静默过）——`docs/198` §3 那一条在 C 上也成立 |
| #152 | 返回类型无法映射（`float`/`signed int`/`long int`）⇒ 整个函数消失，报的是**调用点** |
| #115 | `loment fmt --write` 把声明行与下一行**粘起来**，那一行上的函数随之消失 |

`_C_FN` 锚行首这件事**同一份文件里已经修过一次**：`_C_FIELD`（`potato_from.py:1012-1015`）
的注释写着"**不锚行首**"——先前的 `struct P { int a; int b; };` 只抽到一个字段。
`_C_FN` 是同一处漏网（#122）。

### 3.3 崩 / 把 Python 内部甩到用户面前（3 条）

| # | 症状 |
|---|---|
| #127 | 八进制字面量 `010`：`trans_core.py:533` 的 `int(t[1], 0)`（Python 3 拒绝前导零，而 C 的前导零**就是**八进制）⇒ 裸 `ValueError` 回溯 |
| #137 | 前门用 Python 的 Unicode `\w`、词法用 ASCII ⇒ 前门收下的标识符死在词法上，报在**生成单元**的行号 |
| #116 | `loment doc` 在短 C 单元上抛 `IndexError`（长单元上不崩，但给出**张冠李戴**的文档，见 §3.6）|

### 3.4 报在**别处**（5 条）

| # | 报的说的是 | 真因 |
|---|---|---|
| #131/#133 | 见上（既是静默错，也是同一条式子两个答案）| |
| #152 | "调用了本单元没有的函数 \`f\`" | 三个函数之前那行 `float f()` 没被认 |
| #153 | 原型：`函数 g 重复` / `调用了本单元没有的函数 g` / 一段讲 `--impl` 的话 | 原型这一构造在 Stage A 里没有表示 |
| #154 | 行号 + `len(bodies)` 个函数 | `docs/186` §4 承诺的是**函数名** |
| #141 | "函数 `f` 与**模块** `ma` 重名" | 真冲突是 `ma` 与 `mb` 各发了一个 `f` |
| #146 | `loment dbg` 给的 `文件:行` 不存在（路径丢目录、行号是译文的）| |

### 3.5 文档与实现不符（4 条）

| # | |
|---|---|
| #150 | `docs/186` §4 表达式表列了一元 `~`，实际**拒**（`docs/198` §1 给 Python 改过，C 这张没改）|
| #151 | 同一张表的语句表列了 `e;`，实际只收赋值与调用 |
| #154 | 同一节的"报得出是哪个函数"没兑现 |
| #155 | `loment/examples/multilang/README.md` 还在讲**链接腿**（接口单元 + `.o` + clang），而实现 2026-09-18 已改成**翻译** —— 同目录另两份文件都改了 |

### 3.6 入口面：**不是 C 独有**（4 条，归因很重要）

| # | 症状 | 用别的写法呢 |
|---|---|---|
| #116 | `doc` 崩 / 文档张冠李戴 | Python 同样崩 |
| #115 | `fmt --write` 改坏源 | Python **更重**（7 行塌成 1 行）|
| #142 | LSP 的 `definition` 抛 `LomError` 冲出 handler ⇒ **语言服务进程死** | Python 同样 |
| #144 | 自举 `doc` 退 0 给错文档（module 名成 `grammar`），而同一份文件 `check` 响亮拒（退 1）| 同样 |

**归因**：这几条的根**不在 `ctrans`**，在**"读 L1 源的入口"那一层** ——
`lomfmt` / `lomdoc` / `loment_lsp` 各自 `lex` 原文，**都不走 `potato_from.front_door`**
（`docs/182` §1.9 那张清单）。所以它们**该挂在那里**，不该挂到 C 翻译器上。
C 翻译器的产物在这几处是对的。

**唯一例外是 #145**：`diag` 把 `choose write grammar rust` 说成"**外源 Rust**、别照上面的改"，
而前门认 `rust` 是**原生**基础语法（`NATIVE_GRAMMARS`）—— 那是 `foreign_note` 没跟上
2026-09-22 那条裁定，与上面四条**不同源**，别混成一条。

---

## 4. 为什么现有判据照不到

逐条都能指到一个**具体没被喂过的形状**（issue 里每条都写了），汇总成三类：

1. **语料只用一种形状**：`loment/ctrans/*.c` 四份**一行一个函数、函数之间还有空行**
   （⇒ #122）、**全程只用一种整型**（`numeric.c` 文件头自己写着，⇒ #123/#125/#133）、
   **`static` 出现 0 次**（⇒ #118）、**没有一处拿负值或减法结果去比较**（⇒ #131）、
   **每个函数各占一行**（⇒ #122/#129）、**没有原型**（`grep -c 'int [a-z]*(int [a-z]*);'` = 0，⇒ #153）。
2. **两条入口各测一半**：`loment_ctrans_test` 在**库层面**调 `from_c` 并读 `rep.skipped`
   （所以 #126/#129 那类"scanner 根本没匹配上"它看不见）；走 `front_door` 的判据
   （`loment_grammar_test`）喂的是能翻的源。⇒ #121/#146。
3. **只钉"范围"不钉"内容"**：`test_out_of_subset_is_loud` 的断言是
   `"switch" in msg` 与 `"带正文的" in msg or "1 个" in msg` —— 它的 docstring 复述了
   `docs/186` §4 那句"报得出是哪个函数"，**断言里没有函数名**（⇒ #154）。

还有一类是**判据根本不存在**：#139（两份文件同名 `module`）—— 既有的多模块判据用的是
**两个不同名**的模块；#115/#116/#142/#144 那四条入口面缺陷，**没有一条判据把一份带声明的源
喂给 LSP / fmt / doc**。

---

## 5. 与 `docs/198` 的关系：未修的那三条，今天复核**仍然成立**

`docs/198` §1 已修（`~` 在 Python 那一门），§2 / §3 / §4 当时标"未修"。今天逐条复跑：

| `docs/198` | 今天 | 本轮 issue |
|---|---|---|
| §2 关键词写错静默退回 | **仍成立**（`choose language c` 等四种，`declared=False`，不出声）| **#148** |
| §3 内建"当值用/当语句用"两个答案 | **仍成立**，且**不只 Python** —— 根在共用的 `trans_core.py`（`ty_of` 查 `fns`、`raw` 不查），C 也中 | **#147** |
| §4 `check` 不查 `use` 进来的模块正文 | **仍成立**（坏库当依赖 check 绿、当入口才报）| **#149** |

§3 那一条的措辞把范围写小了（"pytrans 内部不一致"）—— 它的修法在共用核里，改了会同时
影响 C / C++ / Java / C#。**归因的粒度值得在修的时候重新看一眼。**

本轮还**没有**复现 §1 里那种"已修"的回归 —— `~` 在 Python 那一门仍是拒的。

---

## 6. 下一步（建议，不是决定）

按"改一处能让多少条一起消失"排：

1. **一处收口**：`docs/182` §1.9 那张"读 L1 源的入口"清单需要一个**共同的入口函数**
   （`parse_source(text)` 那个方向）—— 认出声明 → 走前门 → 拿到译文与"行号属于哪一份"。
   一次改完，**#115 / #116 / #142 / #144 / #146** 五条同时消失。
2. **判据补语料**，而不是改代码：给 `loment/ctrans/` 加一份**专挑缝**的语料
   （一行两个函数、混宽度、负值比较、局部 `static`、块内同名），
   连同 `test_translation_runs_same_as_clang` 已有的对照面，**#118/#119/#120/#122/#123/#131/#133**
   会一起红。这是**最便宜**的一步。
3. **子集表与实现对齐**（#150/#151/#154）：一张表三处不符，说明它没人校。可以加一条
   判据把 `docs/186` §4 的表与 `ctrans` 的实际接受集**对拍**。
4. **`skip` 报告必须到用户面前**（#126/#129/#152/#121）：`front_door` 扔掉
   `potato_from` 的 report 是既有 **#74**；本轮补的是**用户看到的替代文案是错的**（#152/#153）。
   两处都要对，任一处单独修都不够。
