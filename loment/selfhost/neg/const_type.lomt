module const_type

// 常量类型不是整型 -> E001 (E-TMISMATCH)。常量在 IR 里是立即数, 只能是整型 ——
// 参考实现口径: "常量 C 类型必须是整型，得到 bool"。
// 注意值必须是整数字面量: `const C: bool = true;` 在**解析期**就被拒 (int_lit),
// 到不了检查器, 所以能触发这条规则的只有"类型是 bool、值是整数字面量"这个形状。
const C: bool = 0;
