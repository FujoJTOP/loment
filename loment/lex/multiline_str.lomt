// multiline_str.lomt —— **字符串字面量里有一个真的换行**。
//
// 这不是在演示语言特性 —— 它是一份**语料**，钉住一个下界。
//
// ## 它为什么存在
//
// 2026-09-18 落 S1.2/S1.3 时，自举 driver 里一句 `fail("…\n")` 被 heredoc 腐蚀成了
// **字面换行**。两个词法器都默默收下了，种子照样一致、门禁照样绿 —— 因为**语料里
// 从来没有一份源在字符串里带换行**。形状不存在，判据就测不到它。
//
// 顺手探出来的第一处分歧不在词法器里，在**对照夹具**：`loment_p8_test._python_tokens`
// 当时是从 `(line, col)` **反推**字节偏移的，而它建的行首表把字符串里那个换行也算了
// 一行 —— 于是从那个 token 起，两边偏移整体差 4 字节，**越往后差得越多**。
// 现在夹具直接用词法器的 `off`/`len`（见 `docs/185` §9.3）。
//
// ## 它同时钉住的第二件事
//
// `"a\nb"`（真换行）在 IR 里必须是 `c"\61\0A\62"` —— **十六进制转义**，不能把裸字节
// 写进 LLVM 的字符串常量。`loment_genesis_test` / `loment_dist_test` 会真编它。
//
// ## 已知的语义缺口（**没修，先记着**）
//
// Rust 不许字符串里有裸换行，所以这一份**不是合法 Rust** —— 而 Loment 自称是 Rust 的
// 严格子集。这条是子集性质上的一个洞：未闭合的字符串会报（`未闭合的字符串`），
// 但"中间有换行、后面又有引号"的会被当成一条跨行字符串收下。
// 要堵是**语言面改动**（两个词法器各一条 + 一条反例），不在 S1 这条线上，别顺手做。
module multiline_str

/// 这个字符串的**值**是 3 个字节：`a`、`\n`、`b`。
pub fn width() -> u32 {
    let s: str = "a
b";
    return str_len(s);
}

pub fn _start() {
    // 3 —— 换行是一个字节，不是两个
    syscall4(60, width() as u64, 0, 0);
}
