// ct/nobuiltin.lomt — **表外的名字调不到**（S4.0 语料，S4.2 的域）
//
// expect: ERR nobuiltin
//
// **为什么单独一支**：这是"编译期域"最核心的一条 —— 宏体能做的事**就是内建表里那些**，
// 表外的名字（`syscall4` / 开文件 / `inb` …）根本不是"被检查出来"的，是**调不到**。
//
// 它同时钉住一处**一直存在的两边不一致**：参考侧原先抛的是 `f"nobuiltin:{n}"`，
// 种类名里嵌着内建的名字；自举侧的 `err_name` 是定长码表，只能给 `nobuiltin`。
// 语料里没有一支能走到这条路径，所以"两边种类一致"一直绿 —— 一钉域就露出来了。
// 现取"种类纯粹"：名字属于**消息**（诊断层），不属于种类（契约层）。
module ct_nobuiltin

fn main() -> u64 {
    return syscall4(60, 0, 0, 0);
}
