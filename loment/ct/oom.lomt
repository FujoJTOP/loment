// ct/oom.lomt — 被解释程序的宿主内存**上界**（S4.0 语料）
//
// expect: ERR oom
//
// **为什么单独一支**：自举侧被解释程序的 `alloc` 走 `ar_alloc`，上界是 `ARENA_CAP`(8192)；
// 参考侧原先把它写成 `HEAP = 1 << 20`，于是**两边的 OOM 边界差 128 倍**，而判据全绿
// —— 语料里没有一支能撞到 8192。这与 `load8` 漏偏移（见 `offsets.lomt`）是同一类错：
// **语料没覆盖的形状就是没判据**。
//
// 16（`top` 初值，前 16 字节留给"空指针"）+ 9000 = 9016 > 8192 -> oom。
// 若哪一侧的边界被放宽，这里会变成 `OK 1` —— 一眼能认。
module ct_oom

fn main() -> u64 {
    let p: ptr = alloc(9000);
    store32(p, 0, 1);
    return load32(p, 0) as u64;
}
