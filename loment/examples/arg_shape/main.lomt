// loment/examples/arg_shape/main.lomt —— **数组/切片当泛型实参**（`Box<[u32]>`）。
//
// 它钉的是一格**两边都拒**的边界。2026-09-23 查这条时的顺序值得记 —— **先问"这东西能不能
// 存在"，再吵"它叫什么"**：
//
//   1. 命名：形式对象要求实例名是**标识符**，而 `Box<[u32]>` 按现有规则会拼成 `Box_[u32]` ——
//      **没有名字规则**（嵌套那条 2026-09-23 定了，见 `docs/147` §2；括号这一支没有）。
//   2. **但命名是下游问题**：`Box<ptr>` 是**最好的证据** —— 它的名字完全没问题（`Box_ptr`），
//      可后端照样拒：`native M23: struct Box_ptr 字段类型 ptr 暂只支持标量`。
//      也就是说**结构体字段只支持标量**这条线先卡住了整族形状；名字定不定都编不出能跑的程序。
//   3. 全树**零处**这么写（`grep` 出来的都是 `<<` 移位与注释）。
//
// ⇒ 所以现在的处置是**点名拒**（两个实现都拒，且都是**有名字的拒绝**，不是内部错误 ——
// 参考侧 2026-09-23 之前是"形式对象自检失败"那种崩）。**要收它，先收 M23**：让 struct 字段
// 支持非标量，然后才轮到定名字规则（那时照 `docs/147` 的同一条思路延伸即可）。
//
// 放在 `examples/` 的**子目录**里，与 `nested_gen/` 同一个理由：子目录不进那些按单层 glob
// 扫 `examples/` 的判据。

module arg_shape

struct Box<T> {
    v: T,
}

fn make(a: [u32]) -> u32 {
    let b: Box<[u32]> = Box { v: a };
    return b.v[0];
}
