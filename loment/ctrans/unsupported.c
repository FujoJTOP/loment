/* unsupported.c —— **反例语料**：这份 C 里有 Stage A 子集外的写法。
 *
 * 它存在的唯一理由是让"不静默丢"这条纪律**可被判据钉住**。子集外的写法如果被
 * 悄悄跳过，`lomt_from --impl` 发出来的就是一份**少算了一步却照样能编过**的单元 ——
 * 那是这套工具链反复要消灭的形状（`docs/167`），比"拒绝"坏得多。
 *
 * 这里**一次只放一样**，好让判据指名道姓地断言报的是哪一条。
 */
int with_switch(int x) {
    switch (x) {
        case 0: return 10;
        default: return 20;
    }
}
